services:
  - type: web
    name: sigverify
    runtime: python
    buildCommand: pip install -r requirements.txt && python training.py
    startCommand: gunicorn app:app --timeout 300 --workers 1 --threads 2
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0