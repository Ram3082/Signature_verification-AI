services:
  - type: web
    name: sigverify
    runtime: python
    buildCommand: pip install -r requirements.txt && python training.py
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0